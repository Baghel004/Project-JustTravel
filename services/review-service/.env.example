# Review service configuration (copy to .env and fill in)
PORT=4003

# The review-service owns its OWN database (database-per-service)
REVIEWS_DB_URL=mongodb+srv://<user>:<pass>@<cluster>/wanderlust_reviews?retryWrites=true&w=majority
